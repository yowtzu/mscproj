// expensive function f to every elemetn of a vector, and the vector has prepates
// it is btter to apply to every unique elemetnet, and then copy the result to whoe

n: 100000; vec: n?30 / long vectors with few different values
f:{exp(x*x)}
